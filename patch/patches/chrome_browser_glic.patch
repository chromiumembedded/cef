diff --git chrome/browser/actor/BUILD.gn chrome/browser/actor/BUILD.gn
index 3d7309d6c97f1b6a9f0fb7c679bd7cfdafac2b71..17b54de67f91254ba5aa54a015d3d45aeb1dd451 100644
--- chrome/browser/actor/BUILD.gn
+++ chrome/browser/actor/BUILD.gn
@@ -394,8 +394,6 @@ source_set("browser_tests") {
     ":tools_test_support",
     "ui:event_dispatcher",
     "//chrome/browser/affiliations",
-    "//chrome/browser/glic",
-    "//chrome/browser/glic/test_support",
     "//chrome/browser/optimization_guide",
     "//chrome/browser/optimization_guide:test_support",
     "//chrome/browser/safe_browsing",
@@ -421,7 +419,10 @@ source_set("browser_tests") {
   ]
 
   if (enable_glic) {
-    deps += [ "//chrome/browser/glic" ]
+    deps += [
+      "//chrome/browser/glic",
+      "//chrome/browser/glic/test_support",
+    ]
   }
 
   if (is_android) {
diff --git chrome/browser/contextual_cueing/contextual_cueing_page_data.cc chrome/browser/contextual_cueing/contextual_cueing_page_data.cc
index 81306ac966ccc5046e3a1e117e1a2b1b226f48da..506a86cfe3b00334ea09cab8abd8ad4a3524f81c 100644
--- chrome/browser/contextual_cueing/contextual_cueing_page_data.cc
+++ chrome/browser/contextual_cueing/contextual_cueing_page_data.cc
@@ -8,6 +8,7 @@
 #include "base/strings/string_util.h"
 #include "base/task/single_thread_task_runner.h"
 #include "chrome/browser/contextual_cueing/contextual_cueing_features.h"
+#include "chrome/common/buildflags.h"
 #include "chrome/grit/generated_resources.h"
 #include "content/public/browser/web_contents.h"
 #include "pdf/buildflags.h"
@@ -100,6 +101,7 @@ void ContextualCueingPageData::FindMatchingConfig() {
                                        config.default_text(),
                                        /*is_dynamic=*/true}));
         return;
+#if BUILDFLAG(ENABLE_GLIC)
       } else if (config.has_cue_label()) {
         std::move(cueing_decision_callback_)
             .Run(base::ok(CueingResult{
@@ -108,6 +110,7 @@ void ContextualCueingPageData::FindMatchingConfig() {
                 /*prompt_suggestion=*/"",
                 /*is_dynamic=*/false}));
         return;
+#endif
       }
     } else if (decision == kNeedsPdfPageCount) {
       needs_pdf_page_count = true;
diff --git chrome/browser/profiles/chrome_browser_main_extra_parts_profiles.cc chrome/browser/profiles/chrome_browser_main_extra_parts_profiles.cc
index 290aa9027dd9521cbe501b937680535a77437731..dde4f0efdf9f833c38e6af0887f768014e25145c 100644
--- chrome/browser/profiles/chrome_browser_main_extra_parts_profiles.cc
+++ chrome/browser/profiles/chrome_browser_main_extra_parts_profiles.cc
@@ -522,8 +522,8 @@
 #include "extensions/browser/extensions_browser_client.h"
 #endif
 
-#if BUILDFLAG(ENABLE_GLIC)
 #include "chrome/browser/actor/actor_keyed_service_factory.h"
+#if BUILDFLAG(ENABLE_GLIC)
 #include "chrome/browser/glic/public/glic_enabling.h"
 #include "chrome/browser/glic/public/glic_keyed_service_factory.h"
 #endif
@@ -675,9 +675,7 @@ void ChromeBrowserMainExtraPartsProfiles::
   AccountsPolicyManagerFactory::GetInstance();
   search_integrity::SearchIntegrityFactory::GetInstance();
 #endif
-#if BUILDFLAG(ENABLE_GLIC)
   actor::ActorKeyedServiceFactory::GetInstance();
-#endif
 #if BUILDFLAG(IS_ANDROID)
   AndroidSessionDurationsServiceFactory::GetInstance();
 #endif
diff --git chrome/browser/skills/skills_ui_tab_controller.cc chrome/browser/skills/skills_ui_tab_controller.cc
index 935165a1095b0fa2dcf95a44a46a6a7e70003997..6897ed22b30a5ed6d12d2ea132bce01b3b932910 100644
--- chrome/browser/skills/skills_ui_tab_controller.cc
+++ chrome/browser/skills/skills_ui_tab_controller.cc
@@ -27,13 +27,13 @@
 DEFINE_USER_DATA(skills::SkillsUiTabController);
 
 namespace {
-using glic::mojom::SkillSource;
 
 constexpr base::TimeDelta kNotifyTimeoutSeconds = base::Seconds(60);
 constexpr base::TimeDelta kGlicPanelPollIntervalMilliseconds =
     base::Milliseconds(60);
 
 #if BUILDFLAG(ENABLE_GLIC)
+using glic::mojom::SkillSource;
 glic::mojom::SkillPreviewPtr GetPreviewFromSkill(const skills::Skill& skill) {
   auto skill_preview = glic::mojom::SkillPreview::New();
   skill_preview->id = skill.id;
diff --git chrome/browser/skills/skills_update_observer.cc chrome/browser/skills/skills_update_observer.cc
index 1f5afc4738ed4586467a7c237004012cf6aaa577..f052040f1fa19f049d8a806bf0ecfbb700c3c146 100644
--- chrome/browser/skills/skills_update_observer.cc
+++ chrome/browser/skills/skills_update_observer.cc
@@ -4,8 +4,11 @@
 
 #include "chrome/browser/skills/skills_update_observer.h"
 
+#include "chrome/common/buildflags.h"
+#if BUILDFLAG(ENABLE_GLIC)
 #include "chrome/browser/glic/host/glic_skills_manager.h"
 #include "chrome/browser/glic/public/glic_keyed_service.h"
+#endif
 #include "chrome/browser/optimization_guide/optimization_guide_keyed_service.h"
 #include "chrome/browser/optimization_guide/optimization_guide_keyed_service_factory.h"
 #include "chrome/browser/profiles/profile.h"
@@ -19,6 +22,7 @@
 #include "content/public/browser/navigation_handle.h"
 #include "content/public/browser/web_contents.h"
 
+#if BUILDFLAG(ENABLE_GLIC)
 namespace {
 std::vector<glic::mojom::SkillPtr> ConvertSkillsListToSkills(
     const skills::proto::SkillsList* skills_list) {
@@ -42,6 +46,7 @@ std::vector<glic::mojom::SkillPtr> ConvertSkillsListToSkills(
   return skills;
 }
 }  // namespace
+#endif  // BUILDFLAG(ENABLE_GLIC)
 
 namespace skills {
 
@@ -107,6 +112,7 @@ void SkillsUpdateObserver::OnOptimizationGuideDecision(
 }
 
 void SkillsUpdateObserver::MaybeUpdateContextualSkills() {
+#if BUILDFLAG(ENABLE_GLIC)
   glic::GlicKeyedService* glic_keyed_service = glic::GlicKeyedService::Get(
       Profile::FromBrowserContext(tab_->GetContents()->GetBrowserContext()));
   if (!glic_keyed_service) {
@@ -116,11 +122,14 @@ void SkillsUpdateObserver::MaybeUpdateContextualSkills() {
           glic_keyed_service->GetInstanceForTab(&(*tab_))) {
     instance->host().skills_manager().UpdateSkillPreviews(&(*tab_));
   }
+#endif  // BUILDFLAG(ENABLE_GLIC)
 }
 
+#if BUILDFLAG(ENABLE_GLIC)
 std::vector<glic::mojom::SkillPtr>
 SkillsUpdateObserver::GetContextualSkills() const {
   return ConvertSkillsListToSkills(contextual_skills_.get());
 }
+#endif  // BUILDFLAG(ENABLE_GLIC)
 
 }  // namespace skills
diff --git chrome/browser/skills/skills_update_observer.h chrome/browser/skills/skills_update_observer.h
index 37f108ed7ac4ccadb2e8efbb11f01f2b21b3ced6..12df95df06c1faf7a2f41b8c07d0ac785fbca0aa 100644
--- chrome/browser/skills/skills_update_observer.h
+++ chrome/browser/skills/skills_update_observer.h
@@ -10,8 +10,11 @@
 
 #include "base/memory/raw_ptr.h"
 #include "base/memory/weak_ptr.h"
-#include "chrome/browser/glic/host/glic.mojom.h"
+#include "chrome/common/buildflags.h"
 #include "components/skills/proto/skill.pb.h"
+#if BUILDFLAG(ENABLE_GLIC)
+#include "chrome/browser/glic/host/glic.mojom.h"
+#endif
 #include "content/public/browser/web_contents_observer.h"
 #include "ui/base/unowned_user_data/scoped_unowned_user_data.h"
 
@@ -46,7 +49,9 @@ class SkillsUpdateObserver : public content::WebContentsObserver {
     return contextual_skills_.get();
   }
 
+#if BUILDFLAG(ENABLE_GLIC)
   std::vector<glic::mojom::SkillPtr> GetContextualSkills() const;
+#endif
 
  private:
   void OnOptimizationGuideDecision(
diff --git chrome/browser/ui/tab_search_feature.cc chrome/browser/ui/tab_search_feature.cc
index 41901baa400d7953248f1d64b20fbc2bd8329fa0..75681ca2d32290f13bf5c9d1495f942ce4f785aa 100644
--- chrome/browser/ui/tab_search_feature.cc
+++ chrome/browser/ui/tab_search_feature.cc
@@ -6,12 +6,19 @@
 
 #include <string>
 
+#include "chrome/common/buildflags.h"
+#if BUILDFLAG(ENABLE_GLIC)
 #include "chrome/browser/glic/public/glic_enabling.h"
+#endif
 
 namespace features {
 bool HasTabSearchToolbarButton() {
   // It is important that this value not change at runtime in production. Any
   // future updates to this function must maintain that property.
+#if BUILDFLAG(ENABLE_GLIC)
   return glic::GlicEnabling::IsEnabledByFlags();
+#else
+  return false;
+#endif
 }
 }  // namespace features
diff --git chrome/browser/ui/tabs/BUILD.gn chrome/browser/ui/tabs/BUILD.gn
index bd41fe3c37046518279eb996bedf0c1e1389bf24..000604742755a96446b34611f408210e855ec4c5 100644
--- chrome/browser/ui/tabs/BUILD.gn
+++ chrome/browser/ui/tabs/BUILD.gn
@@ -407,6 +407,7 @@ if (!is_android) {
       "//chrome/common:buildflags",
       "//chrome/common:chrome_features",
       "//components/prefs",
+      "//components/tabs:public",
       "//content/public/browser",
     ]
     if (enable_glic) {
diff --git chrome/browser/ui/tabs/alert/tab_alert_controller.cc chrome/browser/ui/tabs/alert/tab_alert_controller.cc
index ca935e96c558a0c544c16c892269e9227f78f793..52aec1ed6c1ba026371d6fed849f63761111df78 100644
--- chrome/browser/ui/tabs/alert/tab_alert_controller.cc
+++ chrome/browser/ui/tabs/alert/tab_alert_controller.cc
@@ -178,8 +178,8 @@ std::u16string TabAlertController::GetTabAlertStateText(
     case TabAlert::kActorWaitingOnUser:
       return l10n_util::GetStringUTF16(
           IDS_TOOLTIP_TAB_ALERT_STATE_ACTOR_ACCESSING);
-    case TabAlert::kGlicAccessing:
 #if BUILDFLAG(ENABLE_GLIC)
+    case TabAlert::kGlicAccessing:
       return l10n_util::GetStringUTF16(
           IDS_TOOLTIP_TAB_ALERT_STATE_GLIC_ACCESSING);
     case TabAlert::kGlicSharing:
diff --git chrome/browser/ui/tabs/glic_nudge_controller.cc chrome/browser/ui/tabs/glic_nudge_controller.cc
index 0d24a1e1cb7e3d036ac1fc6c9296e4cd0c0b3cef..1677dc26771bd72d76bc9c5159704828b8969b10 100644
--- chrome/browser/ui/tabs/glic_nudge_controller.cc
+++ chrome/browser/ui/tabs/glic_nudge_controller.cc
@@ -7,8 +7,9 @@
 #include "chrome/browser/glic/glic_pref_names.h"
 #include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_interface.h"
-#include "chrome/browser/ui/views/tabs/tab_strip_action_container.h"
+#include "chrome/common/buildflags.h"
 #include "components/prefs/pref_service.h"
+#include "components/tabs/public/tab_interface.h"
 #include "content/public/browser/web_contents.h"
 
 #if BUILDFLAG(ENABLE_GLIC)
diff --git chrome/browser/ui/tabs/tab_strip_api/converters/tab_converters.cc chrome/browser/ui/tabs/tab_strip_api/converters/tab_converters.cc
index 807dfb5de0969f83d5e547d4237d0bb7fb0e9b35..e672452f6862a97c7bcd5ec046ca2927165e66fe 100644
--- chrome/browser/ui/tabs/tab_strip_api/converters/tab_converters.cc
+++ chrome/browser/ui/tabs/tab_strip_api/converters/tab_converters.cc
@@ -6,6 +6,7 @@
 
 #include "base/notimplemented.h"
 #include "base/notreached.h"
+#include "chrome/common/buildflags.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/utf_string_conversions.h"
 #include "chrome/browser/ui/tabs/alert/tab_alert.h"
@@ -204,9 +205,17 @@ tabs::TabAlert FromMojo(mojom::AlertState state) {
     case mojom::AlertState::kVideoRecording:
       return tabs::TabAlert::kVideoRecording;
     case mojom::AlertState::kGlicAccessing:
+#if BUILDFLAG(ENABLE_GLIC)
       return tabs::TabAlert::kGlicAccessing;
+#else
+      NOTREACHED();
+#endif
     case mojom::AlertState::kGlicSharing:
+#if BUILDFLAG(ENABLE_GLIC)
       return tabs::TabAlert::kGlicSharing;
+#else
+      NOTREACHED();
+#endif
     case mojom::AlertState::kActorAccessing:
       return tabs::TabAlert::kActorAccessing;
     case mojom::AlertState::kActorWaitingOnUser:
diff --git chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc
index 43d03b2b996fca77a72e9d1e37c8601f3b4190a1..7d000771ca6b305799fb13c2d000d3ffb496f2c2 100644
--- chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc
+++ chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc
@@ -34,7 +34,9 @@
 #include "chrome/browser/ui/views/tabs/tab_search_button.h"
 #include "chrome/browser/ui/views/tabs/tab_search_container.h"
 #include "chrome/browser/ui/views/tabs/tab_strip.h"
+#if BUILDFLAG(ENABLE_GLIC)
 #include "chrome/browser/ui/views/tabs/tab_strip_action_container.h"
+#endif
 #include "chrome/browser/ui/views/tabs/tab_strip_control_button.h"
 #include "chrome/browser/ui/views/tabs/tab_strip_controller.h"
 #include "chrome/browser/ui/views/tabs/tab_strip_nudge_button.h"
@@ -230,16 +232,20 @@ HorizontalTabStripRegionView::HorizontalTabStripRegionView(
 
   // Add and configure the TabSearchContainer and TabStripComboButton.
   std::unique_ptr<TabSearchContainer> tab_search_container;
+#if BUILDFLAG(ENABLE_GLIC)
   std::unique_ptr<TabStripActionContainer> tab_strip_action_container;
-  if (browser &&
-      (browser->GetType() == BrowserWindowInterface::Type::TYPE_NORMAL)) {
+#endif
+  if (browser && browser->IsNormalBrowser()) {
+#if BUILDFLAG(ENABLE_GLIC)
     if (features::HasTabSearchToolbarButton()) {
       tab_strip_action_container = std::make_unique<TabStripActionContainer>(
           browser, browser->GetFeatures().glic_nudge_controller());
 
       tab_strip_action_container->SetProperty(views::kCrossAxisAlignmentKey,
                                               views::LayoutAlignment::kStart);
-    } else {
+    } else
+#endif
+    {
       tab_search_container = std::make_unique<TabSearchContainer>(
           render_tab_search_before_tab_strip_, this, tab_strip_);
       tab_search_container->SetProperty(views::kCrossAxisAlignmentKey,
@@ -298,18 +304,22 @@ HorizontalTabStripRegionView::HorizontalTabStripRegionView(
         gfx::Insets::TLBR(0, 0, 0,
                           GetLayoutConstant(LayoutConstant::kTabStripPadding)));
   }
+#if BUILDFLAG(ENABLE_GLIC)
   if (tab_strip_action_container) {
     tab_strip_action_container_ =
         AddChildView(std::move(tab_strip_action_container));
   }
+#endif
   UpdateTabStripMargin();
 }
 HorizontalTabStripRegionView::~HorizontalTabStripRegionView() {
   // These objects have pointers to TabStripController, which is also destoroyed
   // by this class. Remove child views that hold raw_ptr to TabStripController.
+#if BUILDFLAG(ENABLE_GLIC)
   if (tab_strip_action_container_) {
     RemoveChildViewT(std::exchange(tab_strip_action_container_, nullptr));
   }
+#endif
   if (new_tab_button_) {
     RemoveChildViewT(std::exchange(new_tab_button_, nullptr));
   }
@@ -378,9 +388,11 @@ views::View::Views HorizontalTabStripRegionView::GetChildrenInZOrder() {
     children.emplace_back(tab_search_container_.get());
   }
 
+#if BUILDFLAG(ENABLE_GLIC)
   if (tab_strip_action_container_) {
     children.emplace_back(tab_strip_action_container_.get());
   }
+#endif
 
   if (reserved_grab_handle_space_) {
     children.emplace_back(reserved_grab_handle_space_.get());
@@ -639,9 +651,11 @@ void HorizontalTabStripRegionView::UpdateButtonBorders() {
   // should be improved, likely by taking the scroll state of the tabstrip into
   // account.
   const auto border_insets = gfx::Insets::TLBR(top_inset, 0, bottom_inset, 0);
+#if BUILDFLAG(ENABLE_GLIC)
   if (tab_strip_action_container_) {
     tab_strip_action_container_->UpdateButtonBorders(border_insets);
   }
+#endif
   if (new_tab_button_) {
     UpdateBorderInsetsIfNeeded(new_tab_button_, border_insets);
   }
diff --git chrome/browser/ui/webui/tab_search/tab_search_page_handler.cc chrome/browser/ui/webui/tab_search/tab_search_page_handler.cc
index 2c1b980c7b0003cfcd7e0daefe10194e2646ec8c..b44b70f081170e8dc4c382048cd644b335172199 100644
--- chrome/browser/ui/webui/tab_search/tab_search_page_handler.cc
+++ chrome/browser/ui/webui/tab_search/tab_search_page_handler.cc
@@ -60,6 +60,7 @@
 #include "chrome/browser/user_education/tutorial_identifiers.h"
 #include "chrome/browser/user_education/user_education_service.h"
 #include "chrome/browser/user_education/user_education_service_factory.h"
+#include "chrome/common/buildflags.h"
 #include "chrome/common/url_constants.h"
 #include "chrome/common/webui_url_constants.h"
 #include "chrome/grit/generated_resources.h"
@@ -1487,8 +1488,11 @@ tab_search::mojom::TabPtr TabSearchPageHandler::GetTab(
                                 alert == tabs::TabAlert::kAudioRecording ||
                                 alert == tabs::TabAlert::kVideoRecording ||
                                 alert == tabs::TabAlert::kAudioPlaying ||
-                                alert == tabs::TabAlert::kAudioMuting ||
-                                alert == tabs::TabAlert::kGlicAccessing;
+                                alert == tabs::TabAlert::kAudioMuting
+#if BUILDFLAG(ENABLE_GLIC)
+                                || alert == tabs::TabAlert::kGlicAccessing
+#endif
+                             ;
                        });
 
   return tab_data;
diff --git chrome/browser/ui/webui/tabs/tabs_mojom_traits.cc chrome/browser/ui/webui/tabs/tabs_mojom_traits.cc
index bf8603c8ffda352771e026ca51426788be18d632..8edeb1c10dcc18f69aef62155567d10e0247283d 100644
--- chrome/browser/ui/webui/tabs/tabs_mojom_traits.cc
+++ chrome/browser/ui/webui/tabs/tabs_mojom_traits.cc
@@ -5,6 +5,7 @@
 #include "chrome/browser/ui/webui/tabs/tabs_mojom_traits.h"
 
 #include "base/notreached.h"
+#include "chrome/common/buildflags.h"
 
 namespace mojo {
 
@@ -142,11 +143,19 @@ bool EnumTraits<MojoTabAlertState, NativeTabAlertState>::FromMojom(
       *out = NativeTabAlertState::kActorWaitingOnUser;
       return true;
     case MojoTabAlertState::kGlicAccessing:
+#if BUILDFLAG(ENABLE_GLIC)
       *out = NativeTabAlertState::kGlicAccessing;
       return true;
+#else
+      return false;
+#endif
     case MojoTabAlertState::kGlicSharing:
+#if BUILDFLAG(ENABLE_GLIC)
       *out = NativeTabAlertState::kGlicSharing;
       return true;
+#else
+      return false;
+#endif
   }
   NOTREACHED();
 }
